# Release Log

Schema: `Date (Timestamp) | Title | Description | New Features`

---

**2026-09-03 (16:18:28 CEST)** | Agent Debate Engine & Live Dashboard Wiring | Pre-launch hardening pass ahead of the PRODUCTION mode cutover: gated trade execution behind a two-agent AI debate, fixed a fundamentals routing gap and a broken portfolio-history query, and brought project documentation up to date. | 
- **Agent Debate Engine (`src/AIEngine.js`, `TradingBot.js`)**: Replaced the single-shot AI call with a two-agent debate (HKUDS/AI-Trader pattern). Agent A (Trader) proposes a setup with a confidence score; Agent B (Risk Auditor) critiques it for slippage, volume, and false-breakout risk and must explicitly approve before `TradingBot.js` will submit an OCO bracket order. Any Agent B rejection or Gemini API error fails closed to HOLD — capital is never deployed on an unaudited proposal.
- **Fundamental Data Fallback (`src/BrokerClient.js`)**: Verified `fetchFundamentals` routes cleanly from Finnhub to `yahoo-finance2` on any Finnhub failure (non-OK response or thrown error), and that the fallback payload is remapped to Finnhub's `{c, d, dp}` shape so downstream callers see a consistent structure. Covered with new fallback tests.
- **Live Dashboard Analytics (`src/DashboardAPI.js`)**: Fixed a `ReferenceError`-causing bug in `/api/v1/portfolio` (`pool.query` on an undefined `pool`) and corrected the closed-trade history query to select `opened_at`/`closed_at`/`net_profit` — the fields `PerformanceMetrics.jsx` actually reads. The equity curve now renders the real trade history instead of always falling back to the sample timeline.
- **Order Safety Guard (`src/BrokerClient.js`)**: `executeBuyOrder`'s share-count guard used `qty < 1`, which is `false` for `NaN` — a corrupted or non-numeric allocation would have silently passed through as a malformed order. Now checked with `!Number.isFinite(qty) || qty < 1`. Found while investigating the `active_capital = NaN` issue below.
- **Testing**: Added `Systest/AIEngine.test.js` (5 tests covering the debate approve/veto/fail-closed paths) and extended `Systest/BrokerClient.test.js` with 3 fundamentals-fallback tests. Full suite: 5 suites / 14 tests passing. Dashboard `npm run lint` and `npm run build` both pass clean.

---

**2026-09-03 (16:41:00 CEST)** | Capital Ledger Repair & Settlement Hardening | Root-caused and repaired a live data corruption incident in `capital_pots`, then closed the code-level gap that caused it. |
- **Data repair (live DB, approved and executed 2026-09-03)**: `capital_pots.active_capital` had been `NaN` since the 2026-09-02 20:15 settlement run. Reconstructed the correct value ($63,237.00) from 13 corroborating `TradingBot` log entries logged in the hours immediately before the corrupting settlement, and confirmed no real trades closed that day (only 5 manually-cleared "ghost position" rows with `net_profit = NULL`). Restored `active_capital = 63237.00` and set those 5 rows' `net_profit = 0.00` in a single transaction.
- **Root-cause fix (`src/Settlement.js`)**: A closed trade with a missing/non-finite `net_profit` (NULL, or Postgres's literal numeric `NaN`) is now skipped from the daily sum and logged as a warning, instead of propagating `NaN` into `dailyNetProfit`. Added a final guard that refuses to write a non-finite figure to `capital_pots` under any circumstance.
- **Testing**: Added `Systest/Settlement.test.js` (3 tests) reproducing the exact historical bug — a NULL or literal-`NaN` `net_profit` row alongside real profit — and asserting the ledger update always receives finite values. Full suite: 6 suites / 17 tests passing.
- **Capacity lock note**: confirmed 3/5 slots are actually held (ISRL, RDAC, RIBBU), not 0/5 as assumed going into this session — worth reconciling against the live Alpaca broker state before launch.

---

**2026-09-11 (04:53:06 CEST)** | OpenRouter Fallback JSON Hardening | The OpenRouter fallback (`meta-llama/llama-3.1-8b-instruct`) was connecting successfully but occasionally returning conversational text (e.g. "The stock...") instead of raw JSON, which crashed `JSON.parse()` with `Unexpected token 'T'... is not valid JSON` and took down Agent A mid-debate. |
- **Prompt hardening (`src/AIEngine.js`)**: Added a strict `STRICT_JSON_DIRECTIVE` to both the Agent A (Trader) and Agent B (Risk Auditor) prompts, explicitly forbidding conversational text, greetings, explanations, or markdown code fences, and requiring the response to begin with `{` and end with `}`.
- **Regex JSON extractor (`src/AIEngine.js`)**: `#generateJSON` now runs every model response through a `extractJSONBlock` regex pass (`/\{[\s\S]*\}/`) after the existing markdown-fence strip, pulling the JSON object out of any remaining conversational filler before `JSON.parse` runs. Applies uniformly to both the Gemini and OpenRouter response paths.
- **Notification/logging strings (`src/AIEngine.js`)**: Added a `console.warn` when extraction actually had to strip non-JSON filler (surfaces flaky-model behavior without failing the call), and reworked the parse-failure path to log a truncated snippet of the raw response and throw a clearer `AIEngine received a non-JSON response from the model` error instead of letting the raw `JSON.parse` `SyntaxError` propagate.
- **Docs**: Updated `README.md`'s `AIEngine.js` description to document the OpenRouter fallback model and the strict-JSON/regex-extraction safety net.

---

**2026-09-14 (23:40:35 CEST)** | DB/Broker Desync Remediation & Reconciliation Routine | Full audit (`PROJECT_ANALYSIS_REPORT_2026-09-14.md`) found the DB fully disconnected from the live Alpaca paper account and 0 trades approved since 2026-09-03. This release fixes every P0/P1/P2 root cause found and ships a reconciliation routine to keep the two in sync going forward. |
- **Volume Data Fix (P1) (`src/BrokerClient.js`)**: `scanMarketMovers` read a nonexistent `mover.volume` field, so every AI prompt saw `Volume: undefined` — the dominant reason Agent B (Risk Auditor) had approved 0 of 38+ proposals since the debate launched. Now reads `volume` directly from the last fetched daily bar (`bars[bars.length - 1].v`).
- **Unprotected Positions Fix (P0) (`src/BrokerClient.js`)**: Bracket orders were submitted with `time_in_force: 'day'`, so stop-loss/take-profit legs silently expired at end-of-session even though positions were held for weeks — confirmed live via RIBBU's stop leg expiring at `2026-09-02T20:00:00Z`. Changed to `'gtc'` so protection lasts for the life of the position.
- **Position Reconciliation (P0) (`src/BrokerClient.js`, new `src/Reconciliation.js`)**: Added `getPositions()` (`GET /v2/positions`) and `getLastFilledOrder()` to `BrokerClient.js`, and a new `Reconciliation.js` module (mirrors `Settlement.js`'s structure) that compares `trade_analytics` against Alpaca's real account state. It only auto-resolves the unambiguous case — a DB row marked `OPEN` for a symbol Alpaca no longer holds at all — using Alpaca's own order history for a verified real fill, then sweeps the P&L into `capital_pots` with the same win/loss distribution rule `Settlement.js` uses. Everything ambiguous (a broker position with no OPEN row, or a symbol with several historical rows) is logged as a flagged anomaly rather than guessed at. Defaults to a dry run; `--apply` writes.
- **Live data repair (executed 2026-09-14, approved before applying)**: Ran `Reconciliation.js --apply` against the live DB. `RIBBU` (id=30) was `OPEN` in the DB but Alpaca's order history showed it was stopped out on 2026-09-02 at a real fill of `$10.07`; closed it with `net_profit = -$1787.52` (verified, not estimated) and deducted the loss from `active_capital` (`$63,237.00` → `$61,449.48`). Separately, `trade_analytics` row 1 (`symbol='NNNN'`, a placeholder from the earlier NaN-corruption bug) had `sell_price`/`net_profit`/`margin_percentage` stored as literal Postgres `NaN` with no recoverable fill data; set to `NULL` to match how other unrecoverable rows in the table already represent "unknown."
- **Flagged, not auto-fixed**: `AAPL`, `SDOT`, `WEAV`, and `WETO` are all live positions on Alpaca with no matching `OPEN` row in `trade_analytics` (three of them — `SDOT`/`WEAV`/`WETO` — were marked `CLOSED` with a fabricated `net_profit = 0.00` in the 2026-09-02 "ghost position" cleanup, but are genuinely still open and, in `WETO`'s case, down ~92%). Each symbol has multiple overlapping historical rows, so there is no single unambiguous row to reopen — this needs a manual per-symbol reconciliation pass, not an automated guess.
- **.env Loading Fix (P2) (`src/Config.js`, `src/BrokerClient.js`, `src/Settlement.js`)**: Replaced the CWD-relative `require('dotenv').config({ path: '../.env' })` with `path.resolve(__dirname, '../.env')`, so a cron/PM2 invocation from any working directory loads this project's own `.env` instead of silently falling back to a stray file elsewhere on disk.
- **AI JSON Fallback Hardening (P1) (`src/AIEngine.js`)**: A `JSON.parse` failure no longer throws immediately. The engine now re-prompts the same OpenRouter fallback model with the exact parse error and the malformed response attached, asking for corrected JSON; if that repair also fails, it escalates once more to a dedicated, more capable model (`meta-llama/llama-3.3-70b-instruct`) before finally giving up.
- **Testing**: Added `Systest/Reconciliation.test.js` (6 tests) and extended `Systest/BrokerClient.test.js` (volume-fix + `gtc` + `getPositions`/`getLastFilledOrder` coverage) and `Systest/AIEngine.test.js` (3 JSON-repair tests). Full suite: 7 suites / 33 tests passing.

---

**2026-09-15 (00:03:43 CEST)** | Full Ledger Cross-Check: Zero Rows Deleted, Three Repaired, Two Reopened | Asked to remove any `trade_analytics` rows that were no longer needed. Cross-checked all 30 rows individually against Alpaca's complete order history (`GET /v2/orders?status=all`, 70 orders total) before touching anything — the finding was that every single row corresponds to a real Alpaca order; nothing was actually junk. What looked like unrecoverable "ghost" corruption was, in every single-lot case, fully recoverable from Alpaca's own records. |
- **`src/BrokerClient.js`**: Added `getOrderHistory(symbol)` (`GET /v2/orders?status=all`) so a row can be reconstructed from its *complete* order history, not just the most recent fill.
- **`src/Reconciliation.js`**: Added a second pass, `#resolveGhostRows`, alongside the existing stale-OPEN pass. It targets any `CLOSED` row with `sell_price IS NULL` — the shared signature of both the 2026-09-02 manual "ghost cleanup" (`net_profit` fabricated to `0.00`) and the older raw-`NaN` corruption row. For each candidate, it only acts when that symbol has **exactly one row anywhere in `trade_analytics`** (multi-row symbols like `WETO` are always left flagged, since Alpaca's average-cost accounting doesn't preserve which buy a remaining share came from — attributing it to one row would be a guess). For a resolvable symbol: if Alpaca's history shows a real filled closing order, the row is repaired with the real sell price/time; if there's no closing fill and the symbol is still a live Alpaca position with matching qty, the row is reopened (`status='OPEN'`) instead, since the `CLOSED` status itself was fabricated.
- **Live ledger repair (executed 2026-09-15, dry-run verified against hand calculations first, then applied)**:
  - `NNNN` (id=1) repaired: real fill $8.40 (was left `NULL` in the 2026-09-14 pass, incorrectly assumed unrecoverable), net **-$257.50**.
  - `FVNNU` (id=26) repaired: real fill $19.02, net **+$1014.81** — this was actually a *winning* trade, previously recorded as a fabricated $0.00 loss.
  - `FVN` (id=27) repaired: real fill $14.25, net **-$2454.01**.
  - `WEAV` (id=5) and `SDOT` (id=25) reopened to `OPEN` — both are still genuinely live Alpaca positions; 2026-09-02's cleanup wrongly closed them.
  - `capital_pots.active_capital`: $61,449.48 → **$59,752.78** (net -$1696.70 across the three repaired closes; reopening doesn't move P&L).
  - Row count before and after: **30 → 30. Zero rows deleted.**
- **Testing**: Added `Systest/Reconciliation.test.js` ghost-repair coverage (4 new tests: reopen path, real-close repair path, multi-row-symbol flagging, and an apply-mode test asserting both a reopen and a close are written with aggregated pot updates) and a `getOrderHistory` test in `Systest/BrokerClient.test.js`. Full suite: 7 suites / 38 tests passing.
- **Still flagged, unresolved**: `WETO` (5 overlapping rows, one live 549-share position — cannot be safely split) and `AAPL` (live position, zero matching row, original trade context unknown). Neither was touched.

---

## ⚠️ Flagged before PRODUCTION cutover — NOT resolved, explicitly deferred

- **`BrokerClient.executeBuyOrder` posts to a hardcoded paper endpoint** (`https://paper-api.alpaca.markets/v2/orders`), and the SDK client is constructed with `paper: true`. Switching `SYSTEM_MODE` to `PRODUCTION` via the dashboard does **not** change where bracket orders are sent — they will still fill on the paper account. Fixing this requires a separate live-trading Alpaca API key pair (the current `.env` only holds paper keys) and a mode-based routing change in `BrokerClient.js`. Discussed 2026-09-03: explicitly deferred at the project owner's request — do not treat `SYSTEM_MODE=PRODUCTION` as executing real capital until this is revisited.
- **`WETO` and `AAPL` need a manual reconciliation pass (flagged 2026-09-14, re-confirmed 2026-09-15)**: `SDOT` and `WEAV` were resolved automatically on 2026-09-15 (see that release entry) since each had exactly one row in `trade_analytics`. `WETO` still cannot be auto-resolved: it has 5 overlapping historical rows (ids 2, 4, 19, 23, 24) sharing one live 549-share Alpaca position (down ~92%, ~$15.5k unrealized as of 2026-09-14), and Alpaca's average-cost accounting doesn't preserve which buy a remaining share came from, so there's no single unambiguous row to reopen. `AAPL` is a live 1-share Alpaca position (opened 2026-09-09) with no matching row in `trade_analytics` at all, and its original trade context/intent is unknown, so it was not auto-inserted. Resolve both by hand against Alpaca's cost-basis/fill data before trusting `capital_pots` or the dashboard's position count.
